apiVersion: v1
kind: Service
metadata:
  name: users-service
  namespace: fcg-apps
  labels:
    app: users-api
spec:
  type: NodePort
  selector:
    app: users-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080